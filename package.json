{
  "name": "r-square-hospitals-journal",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run build --prefix frontend && npm start --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "setup": "npm install --prefix backend && npm install --prefix frontend && node backend/setup-db.js",
    "start": "npm start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
